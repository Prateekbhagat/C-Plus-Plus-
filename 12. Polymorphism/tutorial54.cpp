#include<iostream>
 
using namespace std ;
 
int main()
{
    // ? Polymorphism poly -> many morphism -> forms
    // ! One name and multiple forms means polymorphism 
    // ! Polymorphism two types :
    // ? Compile time polymorphism & Run time polymorphism 
    // ? Compile time Polymorphism :- 1. Function Overloading $ Operator overloading 
    // ! Run time polymorphism :- Virtual Functions
    return 0;
}